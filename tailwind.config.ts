import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.tsx'],
  plugins: [],
  theme: {
    fontFamily: {
      sans: ['Mulish'],
    },
  },
}
export default config
